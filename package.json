{
  "name": "microbit-connection-monorepo",
  "private": true,
  "workspaces": ["packages/*", "apps/*"],
  "scripts": {
    "build": "npm run build:lib && npm run build -w @microbit/microbit-connection-demo && npm run build -w @microbit/microbit-connection-capacitor",
    "build:lib": "npm run build -w @microbit/microbit-connection",
    "dev": "npm run dev -w @microbit/microbit-connection-demo",
    "test": "npm run test -w @microbit/microbit-connection",
    "ci": "npm run build && npm run test && prettier --check packages/microbit-connection/src apps/demo/src apps/capacitor/src",
    "docs": "npm run docs -w @microbit/microbit-connection"
  },
  "devDependencies": {
    "prettier": "3.3.2"
  }
}
